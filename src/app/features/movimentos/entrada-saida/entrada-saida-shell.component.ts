import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-entrada-saida-shell',
  standalone: true,
  imports: [RouterOutlet],
  template: '<router-outlet />'
})
export class EntradaSaidaShellComponent {}
